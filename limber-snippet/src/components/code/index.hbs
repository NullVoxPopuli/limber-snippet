{{#if this.isPreview}}

  <pre><code></code></pre>

{{else}}

  {{#if this.code.isResolved}}
    <iframe
      title={{this.title}}
      src="{{this.host}}?{{this.code.value}}"
      ...attributes
    >
    </iframe>
  {{/if}}

{{/if}}
